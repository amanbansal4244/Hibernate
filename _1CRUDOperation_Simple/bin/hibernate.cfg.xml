<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
          "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
          "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<!-- Generated by MyEclipse Hibernate Tools.                   -->
<hibernate-configuration>

    <session-factory>
    <!-- hbm2ddl.auto property is used to create the table in the database automatically -->
       <!--  <property name="hbm2ddl.auto">update</property>
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>
        <property name="connection.url">jdbc:oracle:thin:@192.168.2.9:1521:XE</property>
        <property name="connection.username">system</property>
        <property name="connection.password">bansal</property> 
        <property name="connection.driver_class">oracle.jdbc.driver.OracleDriver</property>
        -->
        
        <!-- Drop and re-create the database schema on startup 
        If the property “hibernate.hbm2ddl.auto” is set to “create”  This will drop and recreate the database schema on every execution. 
        If it is set to “update” then the database schema will be updated every time rather than dropping and recreating.-->
        <property name="hbm2ddl.auto">update</property>
        
        <!-- Database connection settings -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
    		<property name="hibernate.connection.driver_class">com.mysql.jdbc.Driver</property>
    		<property name="hibernate.connection.url">jdbc:mysql://localhost:3306/sys</property> <!-- sys is schema name  -->
    		<property name="hibernate.connection.username">root</property>
    		<property name="hibernate.connection.password">bansal</property> 
        
        <!-- Echo all executed SQL to stdout
        The show_sql property will print the executed sql in the console when set to true. -->
        <property name="show_sql">true</property>
        
        <!-- JDBC connection pool (use the built-in) -->
 		<property name="connection.pool_size">1</property>
        
        <!-- Under the Mapping resource tag, we need to specify all the mapping class for 
        which we need the table to be created or updated. -->
   		<mapping resource="employee.hbm.xml"/>
    </session-factory>

</hibernate-configuration>